# Base URL of the backend API (see backend/.env.example)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Same Google OAuth Client ID as backend's GOOGLE_CLIENT_ID -- safe to
# expose to the browser (an identifier, not a secret). Leave blank to hide
# the Google button.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
